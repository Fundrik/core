parameters:
    level: max
    paths:
        - src

    bootstrapFiles:
        - phpstan-bootstrap.php

    ignoreErrors:
        - message: '#Method Fundrik\\Core\\Support\\ArrayExtractor::.* should return .* but returns mixed#'
          path: src/Support/ArrayExtractor.php